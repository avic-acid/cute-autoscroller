extends Node2D
#
#var boulder : Resource
#
# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#boulder = load("res://boulder.tscn")
#
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
	#if Input.is_action_just_pressed("spawn-boulder-debug"):
		#var current_boulder = boulder.()
		#add_child(current_boulder)
